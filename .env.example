# Read-only connection to the observatory database.
# Point it at the endpoint that holds the data (the writer branch), never commit
# the real value: it belongs in the hosting platform's environment.
DASHBOARD_DATABASE_URL=postgres://user:password@host.neon.tech/neondb?sslmode=require
